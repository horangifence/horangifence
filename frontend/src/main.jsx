import React from "react";
import ReactDOM from "react-dom/client";
import NewApp from "./NewApp.jsx"; // default import
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NewApp />
  </React.StrictMode>
);
